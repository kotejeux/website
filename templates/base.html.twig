<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    <h1>Kotejeux</h1>
        <nav id="subtitle">
            {% if not is_granted("IS_AUTHENTICATED_FULLY") %}<a href="/login">Login</a>{% endif %}
            {% if is_granted("IS_AUTHENTICATED_FULLY") %}<a href="/logout">Disconnect</a>{% endif %}
        </nav>
        <nav id="sidebar">
            {% block sidebar %}
            <ul>
                <li><a href="{{ path('homepage') }}">Home</a></li>
                <li><a href="{{ path('blog_index') }}">Blog</a></li>
            </ul>
            {% endblock %}
        </nav>

        <div id="content">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
